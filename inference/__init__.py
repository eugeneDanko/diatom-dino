"""Gallery retrieval, open-set decisions, end-to-end inference and reports.

Submodules are not imported eagerly because the pure NumPy decision/index code
is also useful on machines without a functional PyTorch installation.
"""

