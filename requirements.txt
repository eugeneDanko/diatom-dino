numpy>=1.24
pandas>=2.0
Pillow>=10.0
PyYAML>=6.0
# Install the CUDA build of torch/torchvision first from pytorch.org.
# They are intentionally not resolved from the default PyPI index here.
# JupyterLab workflow: jupyterlab>=4.2, ipykernel>=6.29, ipywidgets>=8.1
ultralytics>=8.3
opencv-python>=4.8
scikit-learn>=1.3
matplotlib>=3.7
# Experiment tracking is optional: install clearml>=1.16 when needed.
# Optional diagnostic UMAP plots (PCA is used when omitted):
# umap-learn>=0.5
# Install exactly one FAISS build suitable for the local OS/CUDA environment:
# faiss-cpu>=1.8
# faiss-gpu (when supplied by the CUDA image/conda channel)
